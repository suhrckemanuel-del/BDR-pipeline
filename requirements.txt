# Pinned to versions tested with Python 3.12 on 2026-05-08.
# Bump these intentionally — pydantic and langgraph in particular have churned.

# Core LLM + agent orchestration
anthropic==0.96.0
langgraph==1.1.9
langchain-anthropic==1.4.1
langchain-core==1.3.1
pydantic==2.12.5

# Enrichment integrations
exa-py==2.12.1
requests==2.33.1
notion-client==3.0.0

# UI + data
streamlit==1.56.0
pandas==3.0.2

# Tenant config
PyYAML==6.0.3
python-dotenv==1.2.2

# Windows time zone DB (no-op on macOS / Linux)
tzdata==2026.1; sys_platform == "win32"
